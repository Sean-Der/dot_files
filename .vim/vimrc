colorscheme 256_jungle
syntax on

set nowrap "line wrapping
set title "set the term title
set showcmd "show cur command
set showmatch "show matching bracket
set hidden "no nag screens on tag jump

"Better file handling
set nobackup
set noswapfile
set autowrite

set backspace=indent,eol,start " allow backspacing over everything in insert mode

set hlsearch      " highlight search terms
set incsearch     " show search matches as you type
set ignorecase
set smartcase 

"Tabs
filetype plugin indent on
set smartindent
set tabstop=8
set shiftwidth=8
set expandtab

"Pathogen
call pathogen#infect() 

"Create a mapping for easy ncmpcpp access
nmap mpd :!ncmpcpp -h /home/sean/.mpd/socket<CR><CR>

"Leaving modes in Emacs+EVIL feels right
map <C-g> <Esc>
map! <C-g> <Esc>
cmap <C-g> <Esc>

"better window navigation
map h <C-w>h
map j <C-w>j
map k <C-w>k
map l <C-w>l

let mapleader=","

nmap <leader>r :Ack <C-R><C-W>

"ctags
nmap <leader>tc :!ctags-exuberant --extra=+f -R *<CR><CR>
nmap <leader>tj :tjump <c-r>=expand("<cword>")<CR><CR>
nmap <leader>tr <C-t>

"during split close buffer
nmap <leader>bd :bn <bar> :bd# <CR>

"Command T
noremap <leader>o <Esc>:CommandT<CR>
noremap <leader>b <Esc>:CommandTBuffer<CR>

"Hitting tab in visual mode fixes indentation on the selected lines
vmap <Tab> =

:autocmd BufWritePost *.go :silent Fmt
